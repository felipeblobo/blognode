<div class="container mt-4">

<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
         {{postagem.conteudo}}       
        <hr>
        <small>Data de publicação: {{postagem.date}}</small><br>
    </div>
</div>

</div>