<div class="container pt-5 mt-5 ">

    <div class="card">
        <div class="card-body">
            {{>_msg}}
            <h1>{{postagem.titulo}}</h1>
            <hr>
            {{postagem.conteudo}}
            <hr>
            <small>Data de publicação: {{postagem.date}}</small><br>
        </div>
    </div>

</div>